extends Node
# Here, we'll create the entire machine graph. Then, we'll calculate the position of each Island.
# I'll also implement a BSP graph (or something like that) around the local player.
# It must support multiplayer.
